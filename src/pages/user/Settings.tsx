import { useState } from 'react'
import { Switch } from '../../components/common/FormControls'
import { Card, CardContent, CardHeader, CardTitle } from '../../components/common/Card'
export default function Settings() { const [settings, setSettings] = useState({ email: true, reminders: true, abstracts: true, certificates: true }); const update = (key: keyof typeof settings, value: boolean) => setSettings((current) => ({ ...current, [key]: value })); return <><section className="az-user-page__intro"><p className="az-auth__eyebrow">Settings</p><h2>Preferences</h2><p>Choose how AZTech keeps you informed.</p></section><section className="az-settings"><Card><CardHeader><CardTitle>Notifications</CardTitle></CardHeader><CardContent className="az-settings__controls"><Switch label="Email notifications" checked={settings.email} onChange={(value) => update('email', value)} /><Switch label="Conference reminders" checked={settings.reminders} onChange={(value) => update('reminders', value)} /><Switch label="Abstract updates" checked={settings.abstracts} onChange={(value) => update('abstracts', value)} /><Switch label="Certificate notifications" checked={settings.certificates} onChange={(value) => update('certificates', value)} /></CardContent></Card><Card><CardHeader><CardTitle>Privacy & appearance</CardTitle></CardHeader><CardContent><p className="az-helper">More account and appearance controls will be available when the secure backend profile service is connected.</p></CardContent></Card></section></> }
