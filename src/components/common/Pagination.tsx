import { ChevronLeft, ChevronRight } from 'lucide-react'
interface PaginationProps { page: number; totalPages: number; onPageChange: (page: number) => void }
export function Pagination({ page, totalPages, onPageChange }: PaginationProps) { if (totalPages < 2) return null; const pages = Array.from({ length: totalPages }, (_, index) => index + 1); return <nav className="az-pagination" aria-label="Pagination"><button className="az-page az-page--nav" type="button" disabled={page === 1} onClick={() => onPageChange(page - 1)}><ChevronLeft size={16} />Previous</button>{pages.map((number) => <button key={number} type="button" className={`az-page ${number === page ? 'az-page--active' : ''}`} aria-current={number === page ? 'page' : undefined} onClick={() => onPageChange(number)}>{number}</button>)}<button className="az-page az-page--nav" type="button" disabled={page === totalPages} onClick={() => onPageChange(page + 1)}>Next<ChevronRight size={16} /></button></nav> }
export default Pagination
