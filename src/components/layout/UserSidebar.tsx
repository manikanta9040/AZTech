import { Award, Bell, BookOpen, FileText, LayoutDashboard, LogOut, Settings, UserRound, X } from 'lucide-react'
import { NavLink, useNavigate } from 'react-router-dom'
import { ROUTES } from '../../constants/routes'
import { useAuth } from '../../hooks/useAuth'
const links = [{ to: ROUTES.userDashboard, label: 'Dashboard', icon: LayoutDashboard, end: true }, { to: '/dashboard/profile', label: 'My Profile', icon: UserRound }, { to: '/dashboard/conferences', label: 'My Conferences', icon: BookOpen }, { to: '/dashboard/registrations', label: 'Registrations', icon: FileText }, { to: '/dashboard/abstracts', label: 'Abstracts', icon: FileText }, { to: '/dashboard/certificates', label: 'Certificates', icon: Award }, { to: '/dashboard/notifications', label: 'Notifications', icon: Bell }, { to: '/dashboard/settings', label: 'Settings', icon: Settings }]
export default function UserSidebar({ open, onClose }: { open: boolean; onClose: () => void }) { const { logout } = useAuth(); const navigate = useNavigate(); const leave = () => { logout(); navigate(ROUTES.login, { replace: true }) }; return <><button className={`az-dashboard-overlay ${open ? 'is-open' : ''}`} aria-label="Close navigation" onClick={onClose} /><aside className={`az-user-sidebar ${open ? 'is-open' : ''}`} aria-label="Dashboard navigation"><div className="az-user-sidebar__top"><span className="az-logo">AZ<span>Tech</span></span><button className="az-user-sidebar__close" onClick={onClose} aria-label="Close navigation"><X size={20} /></button></div><nav>{links.map(({ to, label, icon: Icon, end }) => <NavLink key={to} to={to} end={end} onClick={onClose} className={({ isActive }) => `az-user-nav__link ${isActive ? 'is-active' : ''}`}><Icon size={18} />{label}</NavLink>)}</nav><button className="az-user-nav__link az-user-nav__logout" onClick={leave}><LogOut size={18} />Logout</button></aside></> }
