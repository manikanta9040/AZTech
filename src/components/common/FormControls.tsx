import { useId, type InputHTMLAttributes } from 'react'
export function Checkbox({ label, id, ...props }: InputHTMLAttributes<HTMLInputElement> & { label: string }) { const generatedId = useId(); const controlId = id ?? generatedId; return <label className="az-check" htmlFor={controlId}><input id={controlId} type="checkbox" {...props}/>{label}</label> }
export function Radio({ label, id, ...props }: InputHTMLAttributes<HTMLInputElement> & { label: string }) { const generatedId = useId(); const controlId = id ?? generatedId; return <label className="az-check" htmlFor={controlId}><input id={controlId} type="radio" {...props}/>{label}</label> }
export function Switch({ checked, onChange, label, disabled = false }: { checked: boolean; onChange: (checked: boolean) => void; label: string; disabled?: boolean }) { return <label className="az-check"><button type="button" className="az-switch" role="switch" aria-checked={checked} disabled={disabled} onClick={() => onChange(!checked)} /><span>{label}</span></label> }
