import { useEffect, type ReactNode } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { X } from 'lucide-react'
import { Button } from './Button'
type ModalSize = 'sm' | 'md' | 'lg'
interface ModalProps { open: boolean; onClose: () => void; title: string; description?: string; children?: ReactNode; size?: ModalSize; closeOnOverlayClick?: boolean; confirmLabel?: string; onConfirm?: () => void; cancelLabel?: string }
export function Modal({ open, onClose, title, description, children, size = 'md', closeOnOverlayClick = true, confirmLabel, onConfirm, cancelLabel = 'Cancel' }: ModalProps) { useEffect(() => { const onKeyDown = (event: KeyboardEvent) => { if (event.key === 'Escape') onClose() }; if (open) { window.addEventListener('keydown', onKeyDown); return () => window.removeEventListener('keydown', onKeyDown) } }, [open, onClose]); return <AnimatePresence>{open && <motion.div className="az-modal-overlay" role="presentation" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onMouseDown={() => closeOnOverlayClick && onClose()}><motion.section className={`az-modal ${size === 'md' ? '' : `az-modal--${size}`}`} role="dialog" aria-modal="true" aria-labelledby="az-modal-title" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 12 }} onMouseDown={(event) => event.stopPropagation()}><header className="az-modal__header"><div><h2 className="az-modal__title" id="az-modal-title">{title}</h2>{description && <p className="az-modal__description">{description}</p>}</div><button className="az-modal__close" type="button" onClick={onClose} aria-label="Close dialog"><X size={20} /></button></header><div className="az-modal__content">{children}</div>{(onConfirm || cancelLabel) && <footer className="az-modal__footer"><Button variant="ghost" onClick={onClose}>{cancelLabel}</Button>{onConfirm && <Button onClick={onConfirm}>{confirmLabel ?? 'Confirm'}</Button>}</footer>}</motion.section></motion.div>}</AnimatePresence> }
export default Modal
