import { useId, type TextareaHTMLAttributes } from 'react'
interface TextareaProps extends TextareaHTMLAttributes<HTMLTextAreaElement> { label?: string; error?: string; helperText?: string; showCount?: boolean }
export function Textarea({ id, label, error, helperText, required, showCount, maxLength, value, defaultValue, className = '', ...props }: TextareaProps) { const generatedId = useId(); const textareaId = id ?? generatedId; const count = typeof value === 'string' ? value.length : typeof defaultValue === 'string' ? defaultValue.length : 0; return <div className={`az-field ${error ? 'az-field--error' : ''}`}><label className="az-label" htmlFor={textareaId}>{label}{required && <span className="az-required"> *</span>}</label><textarea id={textareaId} required={required} maxLength={maxLength} value={value} defaultValue={defaultValue} aria-invalid={Boolean(error)} className={`az-textarea ${className}`} {...props}/><div className="az-field-meta">{error ? <span className="az-error">{error}</span> : helperText && <span className="az-helper">{helperText}</span>}{showCount && maxLength && <span className="az-helper">{count}/{maxLength}</span>}</div></div> }
export default Textarea
